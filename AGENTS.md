# 开发约定

本文件适用于整个仓库。目标是积累简单、通用、可直接使用的 Go 工具。

## 接手时先读

1. 先读 [utils/README.md](utils/README.md)，了解已有模块，避免重复实现。
2. 使用或修改某个模块前，先读它的 `README.md`，了解接口、默认行为和用法。需要确认实现或排查问题时再读源码。
3. 仓库结构和常用命令见根目录 [README.md](README.md)。

## 代码原则

- 接口简洁，优先直接调用的函数和合理默认值。只有确实需要独立状态或资源生命周期时，才引入实例。
- 先满足当前通用需求，不为假想场景预留复杂配置、接口层、插件机制或多套实现；不够用时再加。
- 封装应实际简化使用，不仅仅把另一个库的接口换个入口暴露出来。
- 新增能力前先查模块索引：已有模块能用就复用；差一点就对现有模块做最小扩展；职责确实不同才新增模块。
- 仅使用 Go 标准库和仓库内的 utils 包，不引入第三方 Go 依赖。
- 全仓库共用根目录的 `go.mod`。命令放在 `cmd/<工具名>/`，通用包放在 `utils/<功能包>/`。
- utils 可以互相引用，但不得循环依赖或依赖 cmd。参数解析、终端交互和进程退出留在 cmd。
- 有共享可变状态时保证并发安全，并在模块文档说明；不要为无共享状态的代码额外加锁。
- 需要报告失败时返回 error，让调用方处理，不在 utils 中调用 `os.Exit` 或 `log.Fatal`。

## 工具安装与来源信任

- 安装、升级或替换任何工具及依赖前，必须先告知用户并取得明确确认。说明用途、是否必需、来源与信任等级、安装位置、主要依赖及替代方案；更换来源或扩大安装范围时重新确认。
- 此要求包括开发和测试工具、浏览器及驱动、全局/项目/临时目录安装、下载解压的可执行文件，以及工具自动下载的组件。临时使用、官方上游发布或任务需要都不能代替用户确认。
- 优先复用已安装工具，避免增加依赖。按以下用户定义的信任顺序选择来源；等级表示用户的来源偏好，不代表安全保证：
  1. 一级：Debian、Arch Linux 官方仓库。优先使用当前系统对应的官方仓库与签名验证；Arch AUR、第三方 APT 仓库不属于此级别。
  2. 二级：Homebrew，仅在一级来源无法满足时考虑，仍需确认。
  3. 三级：PyPI、npm 上极热门且确实必需的包，仅在更高等级来源无法满足时提出，并说明必要性和依赖。通过系统仓库安装 npm/pip，不会使它们下载的包自动成为一级来源。
- PyPI、npm 上非热门或可选的包一律不考虑。不要把“热门”当作免审条件。上游二进制、GitHub 下载、安装脚本、其他包管理器等未列明来源不能自行归级或绕过规则，必须另行说明并取得用户确认。
- 未获确认时可只读查询来源和版本、模拟安装并准备迁移方案；不得实际安装或让测试命令隐式下载工具。迁移通过验证后，只清理本次任务明确创建的旧工具和缓存，不删除其他任务的共享环境。

## 文档随接口维护

- `utils/README.md` 是模块索引的唯一维护位置：每个已有模块一行，包含模块名、一句话用途和模块 README 链接，不列尚未实现的模块。
- 每个模块提供 `utils/<模块名>/README.md`，包含一句话用途、全部导出接口的签名与说明、最小使用示例，以及必要的默认值、错误行为和并发约定。
- 模块 README 应足够让调用方直接使用，无需先通读实现；保持简短，不重复解释实现细节。
- 导出符号保留 Go doc 注释，便于用 `go doc ./utils/<模块名>` 查看。无需提交生成的 Go doc 副本。
- 新增、删除或调整公开接口时，在同一次变更中更新模块文档；模块用途变化时同步索引。

## 完成改动前

1. 确认复用了已有能力，并且没有添加当前用不到的抽象或配置。
2. 为新增行为或修复的问题提供必要测试，重点覆盖边界和并发；不为纯文档改动或简单转发凑测试。
3. Go 代码执行 `go fmt ./...`、`go test ./...`、`go vet ./...` 和 `go build -o ./bin/ ./...`；涉及共享状态或并发时执行 `go test -race ./...`。
4. 修改 webui 的 JS、样式或页面交互时，按 [webui 开发与测试流程](utils/webui/README.md#开发与测试流程) 执行对应请求/浏览器测试；视觉改动补充目视检查。`go test` 不覆盖 `.cjs` 测试，浏览器退出或清理失败也算测试失败。
5. 检查模块索引、接口说明和示例与实际代码一致；纯文档变更检查链接和内容即可。
