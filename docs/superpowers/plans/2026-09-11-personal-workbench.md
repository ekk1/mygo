# 个人工作台实现记录（归档）

2026-09-11 的已完成计划，仅保留交付与验证记录，不作为当前开发指令、安装或提交授权。当前功能、运行方式和测试命令以 [workbench README](../../../cmd/workbench/README.md) 为准；设计取舍见 [设计记录](../specs/2026-09-11-personal-workbench-design.md)。

## 交付范围

`cmd/workbench` 复用 httpserver、webui、kv 和 openai，提供服务商/key 管理、完整模型目录、精选逻辑模型多线路映射、原生工具开关、流式对话、折叠与分支，以及 Files、Containers、Batch、原生操作与日志页面。配置和每个会话独立保存，请求日志强制记录，响应正文可选。

实现和文档通过 055563b、e4230c3 交付，浏览器修复通过 f190849 交付，均已合入并推送 main。

## 验证记录

- 全仓 Go fmt/test/vet/build/race、JS 语法检查及 webui 的 5 项请求测试通过。
- 工作台 6 项 Chromium 测试全部通过，无跳过项：配置与模型映射、失败草稿恢复、流式对话/折叠/分支/日志、资源表单与窄屏布局、文件上传下载和容器/Batch 原生 JSON、主题与手机对话。
- 桌面与手机截图已目视检查，覆盖四套主题及系统明暗偏好（dusk 固定深色）。修复无图片消息显示 null、输入区遮挡回答、会话按钮溢出及深色/手机导航可读性问题。截图是忽略的测试产物，不作为长期文档。
- 初次浏览器启动缺少动态库；用户随后授权 27 个 Debian 官方依赖包。因 sudo 需要密码，包解包到任务临时目录供测试使用，没有写入系统目录。补验保持 sandbox 开启，浏览器退出和测试服务清理成功。
- 所有 API 验证使用本地假服务，未调用真实计费 API。
