# webui 实现记录（归档）

2026-09-09 启动的已完成计划，仅保留历史背景，不作为当前开发指令、安装或提交授权。

初版以 Go 节点组合页面，复用 html/template 转义，内嵌原生 CSS/JS，并提供服务端校验及 303 跳转的表单示例。后续调整为简洁的编辑/预览布局，新增四套独立配色、本地切换和浏览器偏好恢复。

初版验收曾使用 Debian 官方 Node、Playwright 和 Chromium；这不代表当前机器的工具安装状态。当前接口、主题、测试命令与 Playwright 兼容处理统一维护在 [webui README](../../../utils/webui/README.md)，安装约定见 [AGENTS.md](../../../AGENTS.md)。截图属于忽略的构建产物，不作为长期验收记录。
